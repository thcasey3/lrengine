"""
class for injecting the lrdata object into sparks
"""

import os
import pandas as pd

class injectors:
    
    def __init__(self, input_dict):
        
        df = pd.DataFrame(input_dict)
        )
